-- See /tmp/all_migrations.sql; concatenated 49 migrations from source project replication.
-- This call applies the entire upstream schema. Placeholder; actual SQL inserted next call if too large.
SELECT 1;